<h2>Search</h2>

<%= form_tag search_results_path, :method => 'get', :class => 'form-horizontal' do %>
  <%= render :partial => 'form' %>

  <div class="form-actions">
    <%= submit_tag 'Search', :class => 'btn btn-primary' %>
  </div>
<% end %>
